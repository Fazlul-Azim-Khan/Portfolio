/** @type {import('next').NextConfig} */
const nextConfig = {
  // No additional config needed at this stage.
  // This file is the safe place to add image domains,
  // redirects, headers, or env variables later.
}

export default nextConfig
